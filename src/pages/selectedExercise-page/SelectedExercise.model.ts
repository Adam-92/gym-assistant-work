export interface IconsDescription {
  name: string;
  pathImg: string;
  style?: IconsDescriptionStyle
}

export interface IconsDescriptionStyle{
  leftPosition: number,
  topPosition: number,
  width: number
}
